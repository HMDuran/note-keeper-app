import React from 'react';
import AuthForm from './AuthForm';

const SignUp = () => {
  const handleSignUp = () => {
    
  };

  return <AuthForm isSignIn={false} onSubmit={handleSignUp} />;
};

export default SignUp;